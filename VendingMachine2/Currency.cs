// Currency.cs
public class Currency
{
    // Represents the symbol of the currency (£, for example)
    public string Symbol { get; }

    // Constructor to set the currency symbol
    public Currency(string symbol)
    {
        Symbol = symbol;
    }
}
